import './index.css';
import ResetContainer from './containers/ResetContainer';

const index = () => {
    return (
        <div>
            <ResetContainer />
        </div>
    );
};

export default index;
